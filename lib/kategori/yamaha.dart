import 'package:flutter/material.dart';

class YamahaPage extends StatelessWidget {
  const YamahaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Yamaha")),
      body: const Center(
        child: Text("Halaman YAMAHA"),
      ),
    );
  }
}